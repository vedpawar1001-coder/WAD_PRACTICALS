
Assignment no 2c



Open Terminal check the version of node.js
1) npm --version
1) npm install -g @angular/cli@latest
3) ng new part-c (project name)
    (y/n)-y
    (y/n)-y
    (y/n)-y
4) cd part-c (we are in assignment_2c so we have to it with part-c)
you have given three options we should choose option one called css
5) ng serve (it is used to complile the code)
